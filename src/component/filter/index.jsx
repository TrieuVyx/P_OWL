export const getColumnProps = ({
    inputType,
    options,
    ...restProps
}) => ({
    filterDrop: ({
        setSelectedKeys,
        selectedKeys,
        confirm,
        clearFilters
    }) => {
        
    }
})